## EasyCache 릴리스 노트
**Database > EasyCache > 릴리스 노트**

### 2026. 05. 27.

#### 기능 개선/변경
- Valkey 9.0.3을 지원합니다.
- 신규 DB 보안 그룹 규칙의 송신 port 범위를 3306 ~ 43306 에서 10000 ~ 12000 으로 변경합니다.

#### 버그 수정
- valkey 8.1.4 이후 버전에서 불필요한 파라미터인 dynamic-hz 노출을 제거합니다.

### 2026. 03. 24.

#### 기능 개선/변경
- Valkey 8.1.6을 지원합니다.
- 파라미터 그룹과 노드의 redis.conf 설정 차이를 콘솔에서 확인하고, 동기화할 수 있는 기능을 추가했습니다.

### 2026. 01. 27.

#### 기능 개선/변경
- 실제 장애 조치 수행 완료 후 읽기 전용 도메인의 IP 갱신을 위한 버튼을 콘솔에 노출하도록 변경했습니다.
- ID로도 캐시 리스트를 검색할 수 있도록 변경했습니다.
- 사용자 가이드에 포함된 일부 이벤트 목록을 현행화했습니다.
- Valkey 8.x 이상 버전들의 일부 파라미터 이름을 최신 버전으로 변경했습니다.

#### 버그 수정
- 데이터 가져오기에 사용할 RDB 파일명 중 특정 문자열이 포함되었을 경우 오류가 발생하는 버그를 수정했습니다.
- 이벤트 화면에서 다운로드 가능한 CSV 파일 내 헤더 항목의 다국어 표시를 수정했습니다.

### 2025. 11. 15.

#### 기능 개선/변경

- Valkey 8.1.4를 지원합니다.
- 일부 파라미터에 대한 가이드 문구를 개선했습니다.
- 이벤트 구독 > 이벤트 코드 목록을 '시작', '완료', '실패' 순으로 정렬되도록 개선했습니다.

#### 버그 수정

- 특정 조건에서 엔진 버전 업그레이드 시 실패하는 버그를 수정했습니다.
- 캐시, 파라미터 그룹 작업 중일 때 수정 버튼이 활성화되는 버그를 수정했습니다.
- 노드 삭제 중 고가용성 재설정 버튼이 노출되는 버그를 수정했습니다.
- 다른 리전 노드의 모니터링 탭/사용자 탭에서 오류 메시지가 출력되는 버그를 수정했습니다.
- 서버 대시보드 탭에서 다른 리전 노드가 조회되는 버그를 수정했습니다.

### 2025. 09. 23.

#### 기능 개선/변경
- 콘솔 화면을 개편했습니다.
- 기존 기능을 변경 및 개선했습니다.
    - 변경된 기능은 아래와 같습니다.

  | 변경 전 | 변경 후 |
    | --- | --- |
  | 복제 그룹 | 캐시 |
  | 프로필 설정 | 파라미터 그룹 |
  | 접근 제어 기능 | DB 보안 그룹 |
  | 알람 기능 | 알림 그룹 및 사용자 그룹 |

- 백업 및 이벤트 항목 기능을 개선했습니다.

### 2025. 07. 29.

#### 버그 수정
- 상품 활성화 직후 짧은 시간 동안 테넌트 리소스를 찾을 수 없습니다 라는 오류 메시지가 노출되는 버그를 수정했습니다.
- 하이퍼바이저 점검으로 인한 EasyCache 재시작 관련 이메일 발송이 누락되는 버그를 수정했습니다.
- 특정 조건에서 하이퍼바이저 점검으로 인한 EasyCache 재시작이 실패하는 버그를 수정했습니다.

### 2025. 04. 29.

#### 기능 추가
- Valkey 8.0.2를 지원합니다.

#### 기능 변경
- Redis 버전들의 신규 복제 그룹 생성 기능을 종료했습니다.

#### 버그 수정
- 알림 탭에서 대상 복제 그룹에 복제 그룹이 아닌 노드가 노출되는 현상을 수정했습니다.
- 상품 활성화 직후 간헐적인 비정상 오류 메시지 노출 현상을 수정했습니다.
- 상품 활성화 직후 일정 시간 동안 KR1이 아닌 다른 리전에서 복제 그룹 생성을 실패하는 문제를 수정했습니다.

### 2025. 02. 25.

#### 기능 추가
- Redis ACL 기능을 추가했습니다.

#### 버그 수정
- 일부 화면 내 잘못된 버튼명을 수정했습니다.

### 2025. 01. 21.

#### 기능 추가
- Redis 7.2.6을 지원합니다.

#### 버그 수정
- 잘못된 OS 버전 업그레이드 성공 문구가 노출되는 문제를 수정했습니다.
- 사용자 그룹 이름만 변경할 경우 수정 버튼이 활성화되지 않는 문제를 수정했습니다.

### 2024. 11. 26.

#### 기능 개선
- 사용자 그룹 생성 및 수정일의 표시 문자열 및 정렬 방식을 개선했습니다.

#### 버그 수정
- 메트릭 조건이 변경되지 않는 현상을 수정했습니다.

### 2024. 10. 15.

#### 기능 개선
- 알림 그룹 생성 시 이메일 또는 SMS 중 하나를 반드시 선택하도록 안내하는 알림 문구를 추가했습니다.

#### 버그 수정
- 엔진 버전 업그레이드 수행 시 노출되는 상태 문구를 수정했습니다.
- 엔진 버전 업그레이드 요청 직후 노출되는 안내 문구를 수정했습니다.
- 콘솔에서 일부 이벤트 문구가 영어와 일본어로 표시되지 않는 문제를 수정했습니다.

### 2024. 08. 27.

#### 기능 추가

- 서버 대시보드 페이지 화면을 추가했습니다.
- OS 버전 업그레이드 기능을 추가했습니다.
- 알람 전송 시 이벤트 로그 기록 기능을 추가했습니다.

#### 기능 개선/변경
- 기본 OS를 Ubuntu로 변경했습니다.

### 2024. 05. 14.

#### 버그 수정

- 인증서 선택 시 인증서 목록이 일정 높이 이상 확장되지 않는 문제를 수정했습니다.

### 2024. 04. 09.

#### 버그 수정

- 비정상 복제 그룹의 로딩 상태가 무한 지속되는 문제를 수정했습니다.

### 2024. 03. 12.

#### 기능 개선/변경

- Redis 7.2.4를 지원합니다.

### 2024. 02. 22.

#### 기능 개선/변경

- 내부 라이브러리를 교체했습니다.

#### 버그 수정

- 알람 상세 조회 시 일부 메트릭 조건 항목이 정상 표기되지 않는 오류를 수정했습니다.
- 알람 규칙 생성 화면의 이벤트 조건 중복 노출 항목을 수정했습니다.
- 알람 과다 발생 현상을 수정했습니다.
- 특정 조건에서 발생하는 마스터 다운 판정 시간 수정 실패 오류를 수정했습니다.

### 2023. 12. 19.

#### 기능 개선/변경

- 일본(도쿄) 리전 서비스를 종료했습니다.

### 2023. 11. 14.

#### 기능 개선/변경

- 복제 그룹을 생성할 때 일본(도쿄) 리전의 서비스 종료 예정 알림 문구를 추가했습니다.

### 2023. 10. 17.

#### 버그 수정

- 자동 백업의 삭제 시간이 예상 시간과 최대 3시간까지 차이 나는 버그를 수정했습니다.

### 2023. 09. 12.

#### 기능 개선/변경

- 서비스 내부 성능을 개선했습니다.

#### 버그 수정

- Redis 버전 업그레이드 후 프로필 항목이 빈 값으로 노출되는 버그를 수정했습니다.

### 2023. 08. 17.

#### 기능 개선/변경

- TLS 인증서 사용 복제 그룹의 접속용 포트 정보를 TLS 접속용 포트 기준으로 표기하도록 수정했습니다.

#### 버그 수정

- 특정 조건에서 HA 재설정에 실패하는 문제를 수정했습니다.

### 2023. 06. 13.

#### 기능 개선/변경

- Redis 5.0.8 버전의 신규 복제 그룹 생성 기능을 종료했습니다.
- TLS 인증서를 이용한 통신 기능을 추가했습니다.

### 2023. 04. 11.

#### 버그 수정
- 시스템 메모리 메트릭 조건에서 일부 값 구간의 알람이 발생하지 않는 버그를 수정했습니다.

### 2023. 03. 14.

#### 기능 개선
- 서비스 내부 성능을 개선했습니다.

### 2023. 01. 10.

#### 기능 개선
- Redis 7.0.7을 지원합니다.
- Profile에 protected-mode 항목이 표시되도록 개선했습니다.

#### 버그 수정
- 특정 조건에서 버전 업그레이드 재시도에 실패하는 문제를 수정했습니다.

### 2022. 11. 15.

#### 버그 수정
- 접속 정보를 대량으로 등록할 때 오류 메시지가 발생하는 문제를 수정했습니다.
- 노드를 추가하는 과정에서 실패할 경우 HA 복구 기능이 정상적으로 동작하지 않는 문제를 수정했습니다.

### 2022. 10. 11.

#### 기능 개선/변경
- 복제 그룹의 접속을 위한 도메인을 easycache.cloud.toast.com에서 easycache.nhncloudservice.com으로 변경했습니다.

### 2022. 02. 22.

#### 기능 개선
- maxmemory-policy의 기본값을 volatile-lru에서 allkeys-lru로 변경했습니다.

### 2021. 11. 23.

#### 기능 추가
- 복제 그룹을 Redis 버전 5에서 Redis 버전 6로 업그레이드하는 기능을 추가했습니다.

### 2021. 10. 26.

#### 버그 수정
- 데이터 가져오기, 내보내기, 수동 백업 실행 중 프로필 수정을 할 수 없도록 수정했습니다.

### 2021. 09. 28.

#### 기능 개선/변경
- 알람 규칙 생성 화면을 개선했습니다.
- 서비스 이용 개시 처리를 개선했습니다.

#### 버그 수정
- 대용량의 데이터가 있는 복제 그룹에서 노드를 추가할 경우, 노드 생성에 실패하던 문제를 수정했습니다.
- 버전 5인 백업 파일을 신규 복제 그룹으로 복원할 때, 첫 복제 그룹 창에는 버전이 표시되나, 창을 닫았다 다시 열면 버전이 표시되지 않는 문제를 수정했습니다.
- 레플리케이션 이용 불가 상태에서 레플리케이션을 삭제해도 복제 그룹 상태가 정상으로 표시되지 않는 문제를 수정했습니다.
- 복제 그룹 복원에서 복제 그룹이 삭제된 백업을 복원할 때, 선택 가능한 복제 그룹이 표시되지 않는 문제를 수정했습니다.

### 2021. 08. 24.

#### 기능 개선/변경

- Redis 6을 지원합니다.
- 백업을 기존 복제 그룹에 복원하는 기능을 추가했습니다.
- 알람을 설정할 때 알람 템플릿을 사용해서 알람 발생 조건을 설정할 수 있는 기능을 추가했습니다.
- **접속 제어 정보**의 기본 네트워크는 삭제할 수 없도록 변경했습니다.

### 2021. 07. 27.

#### 기능 개선/변경

- EasyCache 데이터를 Object Storage로 내보내는 기능(export)을 추가했습니다.
- **마스터 승격**을 **마스터 변경**으로 수정하고 드롭다운 메뉴로 이동했습니다.

#### 버그 수정

- 접속 제어 정보에서 등록 실패한 CIDR이 중복되어 표시되는 현상을 수정했습니다.

### 2021. 06. 29.

#### 기능 개선/변경

- EasyCache 모니터링의 상세 화면에서 Bytes 단위 그래프의 경우 Bytes, MB, GB 중에서 선택하는 기능을 추가했습니다.
- EasyCache 모니터링의 상세 화면에서 표시될 노드 선택 기능을 추가했습니다.
- 외부에서 사용하던 Redis의 데이터를 EasyCache로 가져오기(import) 기능을 추가했습니다.
- 읽기 기능 분산을 위한 Multi Replica 기능을 추가했습니다.
- 이벤트 검색 기간 캘린더 오른쪽 화면에 현재 시간을 추가했습니다.

#### 버그 수정

- 복제 그룹 프로필 수정 실패 창에 이름이 표시되도록 수정했습니다.
- 복제 그룹 노드를 삭제하고 다른 복제 그룹 노드 삭제 버튼을 클릭하면 노드를 선택하지 않을 경우 삭제 버튼이 비활성화되는 현상을 수정했습니다.

### 2021. 05. 25.

#### 기능 개선/변경

- 모니터링에서 전체 화면 크기에 따라 각 차트 크기도 조정되도록 개선했습니다.
- 모니터링 데이터 보존 기간을 1개월에서 40일로 변경했습니다.

#### 버그 수정

- 로그 보기에서 검색 기간을 지정하는 경우 오류 메시지가 표시되는 현상을 수정했습니다.

### 2021. 04. 27.

#### 기능 개선/변경

- 이용 불가 상태에서는 삭제, 노드 삭제, 재시작 기능만 사용할 수 있도록 변경했습니다.
- 이벤트 카테고리 및 검색 시간 버튼 선택 시 자동으로 검색되도록 수정했습니다.
- 알람 규칙에서 알람 이벤트, 복제 그룹, 수신 그룹을 검색한 후에 하위 목록을 선택할 때, 검색어가 유지되지 않도록 수정했습니다.

### 2021. 03. 23.

#### 기능 개선/변경

- 읽기 전용 도메인 이벤트 등록을 추가했습니다.
- 공인 도메인 설정 시 인터넷 게이트웨이가 없는 서브넷을 이용하고 있는 복제 그룹의 경우에 오류 메시지를 표시하도록 수정했습니다.

#### 버그 수정

- 로그 검색에서 사용자가 검색어로 공백을 입력했을 때 오류가 발생하지 않도록 수정했습니다.

### 2021. 02. 23.

#### 기능 개선/변경

- 읽기 전용 도메인 툴팁을 추가했습니다.

#### 버그 수정

- 프로필 최대 입력값을 입력할 경우 복제 그룹이 생성되지 않는 현상을 수정했습니다.

### 2021. 01. 26.

#### 기능 개선/변경

- Maxmemory 기본값을 가용 메모리의 70%에서 50%로 변경했습니다.
- 프로필에 **activedefrag** 관련 항목을 추가했습니다.
- **로그 보기** 대화 상자 UI를 개선했습니다.

#### 버그 수정

- 모니터링에서 시간을 변경하면 날짜가 1월 2일로 변경되는 현상을 수정했습니다.

### 2020. 12. 29.

#### 기능 추가

- 읽기 전용 도메인 기능을 추가했습니다.

#### 기능 개선/변경

- 접속 제어 정보에 관련된 기능을 개선했습니다.
- 인스턴스 타입 변경 버튼 위에 마우스를 올리면 안내 사항이 나타나도록 개선했습니다.
- 대화 상자 버튼 이름을 OK에서 확인으로 수정했습니다.

#### 버그 수정

- 복제 그룹의 프로필 변경 중에 변경 전 프로필을 수정하면 프로필 수정에 실패하여 이용 불가가 되는 현상을 수정했습니다.
- 홈페이지 언어를 일본어로 설정했을 때 모니터링 필터 옵션이 한글로 보이던 것을 일본어로 보이도록 수정했습니다.

### 2020. 11. 24.

#### 기능 개선/변경

- 로그 전체 화면 보기를 추가했습니다.

### 2020. 10. 27.

#### 버그 수정
- 복제 그룹 이름 변경 시 노드 이름도 변경된 그룹 이름이 반영되도록 수정했습니다.
- CPU, RAM의 쿼터를 넘어설 경우의 오류 메시지를 수정했습니다.

### 2020. 09. 22.

#### 기능 개선/변경

- 한국(평촌) 리전을 오픈했습니다.
- 로그 확인 기능을 추가했습니다.
- VPC 서브넷 목록을 이름순으로 표시합니다.
- 서비스 이용 권한에 대응했습니다.

#### 버그 수정

- 모니터링 그래프의 단위를 수정했습니다.
    - 입력 바이트: byte -> bytes/1min
    - 출력 바이트: byte -> bytes/1min
    - 초당 처리한 명령 수: counts/seconds -> count/1sec
- 프로필을 수정할 때, 수정 중인 프로필을 사용하고 있는 복제 그룹의 상태가 프로필 수정 중으로 표시되지 않고 정상으로 표시되는 현상을 수정했습니다.

### 2020. 08. 25.

#### 기능 개선/변경

- 복제 그룹을 생성할 때 Redis 접속용 비밀번호 설정 여부를 옵션으로 선택할 수 있도록 변경했습니다.

#### 버그 수정

- 레플리카 노드를 승격할 때 노드 승격 완료 시점과 상태 표시가 일치하지 않는 현상을 수정했습니다.

### 2020. 07. 28.

#### 기능 추가

- 인스턴스 타입 변경 기능을 추가했습니다.

#### 기능 개선/변경

- 지원되는 인스턴스 타입에서 Compute Optimized 유형을 삭제했습니다.

#### 버그 수정

- 모니터링 그래프에서 연결된 클라이언트가 누적으로 표시되는 현상을 수정했습니다.
- 유저 정의 프로필을 사용하던 Redis 3.2.12 복제 그룹의 백업에서 복원할 때 Redis 3.2 기본 프로필로 복원되던 버그를 수정했습니다.

### 2020. 06. 23.

#### 기능 개선/변경

- CloudTrail 서비스에 이벤트를 등록했습니다.

#### 버그 수정

- 노드가 재기동될 경우 INFO의 Redis 서버 가동 시간(uptime\_in\_seconds)이 실제보다 9시간 더해진 값으로 출력되는 현상을 수정했습니다.
- 접속 정보 탭에서 패스워드를 확인하려고 보기 버튼을 클릭하여도 노드가 장애 상태인 경우에는 패스워드 습득에 실패하는 현상을 수정했습니다.
- HA 재설정 버튼이 표시되어 HA 재설정을 수행했으나 실패하는 현상을 수정했습니다.
- 모니터링 화면의 검색 기간에서 지정을 선택하여도 기간을 자유롭게 설정할 수 없는 현상을 수정했습니다.

### 2020. 05. 26.

#### 기능 개선/변경

- Redis 5.0을 지원합니다.
- 프로필 복사 기능을 제공합니다.
- 모니터링을 개선했습니다.
    - 조회 탭을 복제 그룹 상세 화면에서 초기 화면 상단의 별도 탭으로 이동했습니다.
    - 현재 시간 버튼을 추가했습니다.
    - 자동 갱신 체크박스를 추가했습니다.
    - 복제 그룹 선택 드롭다운을 추가했습니다.
    - 단일 그래프를 팝업으로도 표시하여 통계 항목과 집계 기간을 선택할 수 있도록 개선했습니다.
- 프로필을 생성하거나 수정할 때 상세 설정을 통해 설정 값을 수정할 수 있도록 변경했습니다.

#### 버그 수정

- 노드가 추가된 복제 그룹 수정 후 노드를 추가하지 않은 복제 그룹을 수정하려고 하면 수정 버튼이 비활성화되는 현상을 수정했습니다.
- 모니터링 항목에서 연결된 클라이언트 수와 블록된 클라이언트 수가 누적 값으로 표시되는 버그를 수정했습니다.
- 프로필 목록 수정 후 프로필 목록이 일부밖에 표시되지 않는 현상을 수정했습니다.
- 복제 그룹에서 사용 중인 프로필을 수정하는 경우, 수정 후 화면이 리로드되지 않는 현상을 수정했습니다.

### 2020. 04. 28.

#### 기능 개선

- HA 감시 설정에 헬스 체크 응답 시간을 사용자가 설정할 수 있도록 기능을 제공합니다.
- 마스터 수동 승격 기능을 제공합니다.
- 모니터링 항목에서 조회 성공, 실패 수를 누적 값이 아닌 시간별 값으로 표시하도록 변경했습니다.
- 알람 규칙에서 메모리 사용량을 %로 설정할 수 있도록 변경했습니다.

#### 버그 수정

- 10분 단위로 모니터링에서 그래프가 잘려서 나오는 현상을 수정했습니다.
- 장애 조치 후 장애 노드가 수 초 이내에 복구된 경우에 복제 그룹, 노드 상태가 정상적으로 반영되지 않는 현상을 수정했습니다.
- 경고 상태 복제 그룹의 패스워드 변경을 가능하게 변경했습니다.
- 일부 메뉴와 라벨을 수정했습니다.

### 2020. 03. 24.

#### 기능 개선

- EasyCache 기본 config 값을 변경했습니다(tcp-keepalive[0→300]).

### 2020. 02. 25.

#### 기능 개선

- 접속 정보를 동시에 여러 개 추가할 수 있도록 수정했습니다.
- SMS 내용을 개선했습니다.
- 중복된 CIDR 입력 시 메시지를 수정했습니다.

#### 버그 수정
- 대용량 데이터가 있을 때 레플리카 노드를 추가할 경우 데이터 동기화 무한 루프가 발생하는 문제를 수정했습니다.
- 복제 그룹 내 노드 생성 실패 시 노드 목록에서 노드가 계속 생성 중으로 표시되는 문제를 수정했습니다.

### 2020. 02. 11.

#### 기능 개선

- TOAST 회원 인증 모듈 버전을 업그레이드했습니다.

#### 버그 수정
- 복제 그룹 작성 시 인터넷 게이트웨이를 설정하지 않은 사용자의 VPC 서브넷을 선택하는 경우 실패하는 현상을 수정했습니다.

### 2020. 01. 21.

#### 기능 개선

- 콘솔 화면과 이벤트 메시지에 대해 일본어를 지원합니다.
- 장애 조치(failover) 후 도메인 변경 실패 시 이벤트 등록을 추가했습니다.

### 2019. 12. 24.

#### 신규 서비스 출시

- TOAST EasyCache는 Redis(REmote DIctionary Server)를 클라우드 환경에서 제공하는 서비스입니다.
