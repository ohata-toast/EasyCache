## EasyCacheリリースノート
**Database > EasyCache > リリースノート**

### 2026. 05. 27.

#### 機能改善・変更
- Valkey 9.0.3をサポートします。

#### 不具合の修正
- valkey 8.1.4以降のバージョンで、不要なパラメーターであるdynamic-hzの表示を削除します。

### 2026. 03. 24.

#### 機能の改善・変更
- Valkey 8.1.6をサポートします。
- パラメータグループとノードのredis.conf設定の差をコンソールで確認し、同期できる機能を追加しました。

### 2026. 01. 27.

#### 機能の改善・変更
- 実際のフェイルオーバー実行完了後、読み取り専用ドメインのIP更新のためのボタンをコンソールに表示するように変更しました。
- IDでもキャッシュリストを検索できるように変更しました。
- ユーザーガイドに含まれる一部のイベント一覧を最新化しました。
- Valkey 8.x以降バージョンのいくつかのパラメータ名を最新バージョンに変更しました。

#### 不具合の修正
- データのインポートに使用するRDBファイル名の中に特定の文字列が含まれている場合、エラーが発生するバグを修正しました。
- イベント画面からダウンロード可能なCSVファイル内のヘッダ項目の多言語表示を修正しました。

### 2025. 11. 15.

#### 機能の改善・変更

- Valkey 8.1.4をサポートします。
- 一部のパラメータに対するガイド文言を改善しました。
- イベント購読 > イベントコード一覧が「開始」、「完了」、「失敗」の順にソートされるように改善しました。

#### 不具合の修正

- 特定の条件でエンジンバージョンのアップグレード時に失敗するバグを修正しました。
- キャッシュ、パラメータグループのタスク中に修正ボタンが有効になるバグを修正しました。
- ノード削除中に高可用性リセットボタンが表示されるバグを修正しました。
- 他のリージョンノードのモニタリングタブ/ユーザータブでエラーメッセージが出力されるバグを修正しました。
- サーバーダッシュボードタブで他のリージョンノードが照会されるバグを修正しました。

### 2025. 09. 23.

#### 機能の改善・変更
- コンソール画面をリニューアルしました。
- 既存の機能を変更及び改善しました。
- 変更された機能は次のとおりです。

| 変更前 | 変更後 |
    | --- | --- |
  | レプリケーショングループ | キャッシュ |
  | プロフィール設定 | パラメータグループ |
  | アクセス制御機能 | DBセキュリティグループ |
  | アラーム機能 | 通知グループ及びユーザーグループ |

- バックアップ及びイベント項目機能を改善しました。

### 2025. 07. 29.

#### 不具合の修正
- 商品の有効化直後、短時間テナントリソースが見つかりませんというエラーメッセージが表示されるバグを修正しました。
- ハイパーバイザーのメンテナンスによるEasyCache再起動に関するメール送信が漏れるバグを修正しました。
- 特定の条件でハイパーバイザーのメンテナンスによるEasyCache再起動が失敗するバグを修正しました。

### 2025. 04. 29.

#### 機能の追加
- Valkey 8.0.2をサポートします。

#### 機能の変更
- Redisバージョンの新規レプリケーショングループ作成機能を終了しました。

#### 不具合の修正
- 通知タブで対象のレプリケーショングループに、レプリケーショングループではなくノードが表示される現象を修正しました。
- 商品の有効化直後、まれに異常なエラーメッセージが表示される現象を修正しました。
- 商品の有効化直後、一定期間KR1以外の他のリージョンでレプリケーショングループの作成に失敗する問題を修正しました。

### 2025. 02. 25.

#### 機能の追加
- Redis ACL機能を追加しました。

#### 不具合の修正
- 一部の画面内の誤ったボタン名を修正しました。

### 2025. 01. 21.

#### 機能の追加
- Redis 7.2.6をサポートします。

#### 不具合の修正
- 誤ったOSバージョンアップグレード成功の文言が表示される問題を修正しました。
- ユーザーグループ名のみ変更した場合、修正ボタンが有効にならない問題を修正しました。

### 2024. 11. 26.

#### 機能の改善
- ユーザーグループの作成及び修正日の表示文字列とソート方式を改善しました。

#### 不具合の修正
- メトリック条件が変更されない現象を修正しました。

### 2024. 10. 15.

#### 機能の改善
- 通知グループ作成時、メールまたはSMSのいずれかを必ず選択するように案内する通知文言を追加しました。

#### 不具合の修正
- エンジンバージョンのアップグレード実行時に表示されるステータスの文言を修正しました。
- エンジンバージョンのアップグレードリクエスト直後に表示される案内文言を修正しました。
- コンソールで一部のイベント文言が英語と日本語で表示されない問題を修正しました。

### 2024. 08. 27.

#### 機能の追加

- サーバーダッシュボードページ画面を追加しました。
- OSバージョンのアップグレード機能を追加しました。
- アラーム送信時のイベントログ記録機能を追加しました。

#### 機能の改善・変更
- デフォルトOSをUbuntuに変更しました。

### 2024. 05. 14.

#### 不具合の修正

- 証明書選択時、証明書一覧が一定の高さ以上拡張されない問題を修正しました。

### 2024. 04. 09.

#### 不具合の修正

- 異常なレプリケーショングループのローディング状態が無限に続く問題を修正しました。

### 2024. 03. 12.

#### 機能の改善・変更

- Redis 7.2.4をサポートします。

### 2024. 02. 22.

#### 機能の改善・変更

- 内部ライブラリを交換しました。

#### 不具合の修正

- アラーム詳細照会時、一部のメトリック条件項目が正常に表記されないエラーを修正しました。
- アラームルール作成画面のイベント条件で重複表示される項目を修正しました。
- アラームの過多発生現象を修正しました。
- 特定の条件で発生するマスターダウン判定時間の修正失敗エラーを修正しました。

### 2023. 12. 19.

#### 機能の改善・変更

- 日本(東京)リージョンのサービスを終了しました。

### 2023. 11. 14.

#### 機能の改善・変更

- レプリケーショングループを作成する際に、日本(東京)リージョンのサービス終了予定を通知する文言を追加しました。

### 2023. 10. 17.

#### 不具合の修正

- 自動バックアップの削除時間が予想時間と最大3時間まで差が出るバグを修正しました。

### 2023. 09. 12.

#### 機能の改善・変更

- サービス内部のパフォーマンスを改善しました。

#### 不具合の修正

- Redisバージョンのアップグレード後、プロフィール項目が空の値として表示されるバグを修正しました。

### 2023. 08. 17.

#### 機能の改善・変更

- TLS証明書を使用するレプリケーショングループの接続用ポート情報をTLS接続用ポート基準で表記するように修正しました。

#### 不具合の修正

- 特定の条件でHAリセットに失敗する問題を修正しました。

### 2023. 06. 13.

#### 機能の改善・変更

- Redis 5.0.8バージョンの新規レプリケーショングループ作成機能を終了しました。
- TLS証明書を利用した通信機能を追加しました。

### 2023. 04. 11.

#### 不具合の修正
- システムメモリのメトリック条件で一部の値の区間のアラームが発生しないバグを修正しました。

### 2023. 03. 14.

#### 機能の改善
- サービス内部のパフォーマンスを改善しました。

### 2023. 01. 10.

#### 機能の改善
- Redis 7.0.7をサポートします。
- Profileにprotected-mode項目が表示されるように改善しました。

#### 不具合の修正
- 特定の条件でバージョンのアップグレード再試行に失敗する問題を修正しました。

### 2022. 11. 15.

#### 不具合の修正
- 接続情報を大量に登録する際にエラーメッセージが発生する問題を修正しました。
- ノードを追加する過程で失敗した場合、HA復旧機能が正常に動作しない問題を修正しました。

### 2022. 10. 11.

#### 機能の改善・変更
- レプリケーショングループの接続のためのドメインをeasycache.cloud.toast.comからeasycache.nhncloudservice.comに変更しました。

### 2022. 02. 22.

#### 機能の改善
- maxmemory-policyのデフォルト値をvolatile-lruからallkeys-lruに変更しました。

### 2021. 11. 23.

#### 機能の追加
- レプリケーショングループをRedisバージョン5からRedisバージョン6へアップグレードする機能を追加しました。

### 2021. 10. 26.

#### 不具合の修正
- データインポート、エクスポート、手動バックアップの実行中はプロフィールを修正できないように修正しました。

### 2021. 09. 28.

#### 機能の改善・変更
- アラームルール作成画面を改善しました。
- サービス利用開始処理を改善しました。

#### 不具合の修正
- 大容量のデータがあるレプリケーショングループでノードを追加する場合、ノードの作成に失敗していた問題を修正しました。
- バージョン5であるバックアップファイルを新規レプリケーショングループに復元する際、最初のレプリケーショングループのウィンドウにはバージョンが表示されるが、ウィンドウを閉じて再度開くとバージョンが表示されない問題を修正しました。
- レプリケーション利用不可の状態でレプリケーションを削除しても、レプリケーショングループの状態が正常に表示されない問題を修正しました。
- レプリケーショングループの復元で、レプリケーショングループが削除されたバックアップを復元する際、選択可能なレプリケーショングループが表示されない問題を修正しました。

### 2021. 08. 24.

#### 機能の改善・変更

- Redis 6をサポートします。
- バックアップを既存のレプリケーショングループに復元する機能を追加しました。
- アラームを設定する際、アラームテンプレートを使用してアラーム発生条件を設定できる機能を追加しました。
- **接続制御情報**のデフォルトネットワークは削除できないように変更しました。

### 2021. 07. 27.

#### 機能の改善・変更

- EasyCacheデータをObject Storageにエクスポートする機能(export)を追加しました。
- **マスター昇格**を**マスター変更**に修正し、プルダウンメニューに移動しました。

#### 不具合の修正

- 接続制御情報で登録に失敗したCIDRが重複して表示される現象を修正しました。

### 2021. 06. 29.

#### 機能の改善・変更

- EasyCacheモニタリングの詳細画面でBytes単位のグラフの場合、Bytes、MB、GBの中から選択する機能を追加しました。
- EasyCacheモニタリングの詳細画面で表示されるノードの選択機能を追加しました。
- 外部で使用していたRedisのデータをEasyCacheにインポートする(import)機能を追加しました。
- 読み取り機能分散のためのMulti Replica機能を追加しました。
- イベント検索期間カレンダーの右側の画面に現在時間を追加しました。

#### 不具合の修正

- レプリケーショングループプロフィールの修正失敗ウィンドウに名前が表示されるように修正しました。
- レプリケーショングループのノードを削除し、他のレプリケーショングループのノード削除ボタンをクリックすると、ノードを選択しない場合削除ボタンが無効になる現象を修正しました。

### 2021. 05. 25.

#### 機能の改善・変更

- モニタリングで全体画面のサイズに合わせて各チャートのサイズも調整されるように改善しました。
- モニタリングデータの保存期間を1か月から40日に変更しました。

#### 不具合の修正

- ログ表示で検索期間を指定した場合にエラーメッセージが表示される現象を修正しました。

### 2021. 04. 27.

#### 機能の改善・変更

- 利用不可状態では削除、ノード削除、再起動機能のみ使用できるように変更しました。
- イベントカテゴリー及び検索時間ボタンを選択した際、自動的に検索されるように修正しました。
- アラームルールでアラームイベント,レプリケーショングループ,受信グループを検索した後,下位リストを選択する際に検索ワードが保持されないように修正しました。

### 2021. 03. 23.

#### 機能の改善・変更

- 読み取り専用ドメインイベントの登録を追加しました。
- 公認ドメインの設定時、インターネットゲートウェイがないサブネットを利用しているレプリケーショングループの場合にエラーメッセージを表示するように修正しました。

#### 不具合の修正

- ログ検索でユーザーが検索ワードとして空白を入力した際にエラーが発生しないように修正しました。

### 2021. 02. 23.

#### 機能の改善・変更

- 読み取り専用ドメインのツールチップを追加しました。

#### 不具合の修正

- プロフィールの最大入力値を入力した場合、レプリケーショングループが作成されない現象を修正しました。

### 2021. 01. 26.

#### 機能の改善・変更

- Maxmemoryのデフォルト値を可用メモリの70%から50%に変更しました。
- プロフィールに**activedefrag**関連の項目を追加しました。
- **ログ表示**ダイアログUIを改善しました。

#### 不具合の修正

- モニタリングで時間を変更すると日付が1月2日に変更される現象を修正しました。

### 2020. 12. 29.

#### 機能の追加

- 読み取り専用ドメイン機能を追加しました。

#### 機能の改善・変更

- 接続制御情報に関連する機能を改善しました。
- インスタンスタイプの変更ボタンの上にマウスを乗せると案内事項が表示されるように改善しました。
- ダイアログボタンの名前をOKから確認に修正しました。

#### 不具合の修正

- レプリケーショングループのプロフィール変更中に変更前のプロフィールを修正すると、プロフィールの修正に失敗して利用不可になる現象を修正しました。
- Webサイトの言語を日本語に設定した際、モニタリングフィルタのオプションが韓国語で表示されていたものを日本語で表示されるように修正しました。

### 2020. 11. 24.

#### 機能の改善・変更

- ログの全体画面表示を追加しました。

### 2020. 10. 27.

#### 不具合の修正
- レプリケーショングループ名の変更時、ノード名にも変更されたグループ名が反映されるように修正しました。
- CPU、RAMのクォータを超過した場合のエラーメッセージを修正しました。

### 2020. 09. 22.

#### 機能の改善・変更

- 韓国(ピョンチョン)リージョンをオープンしました。
- ログ確認機能を追加しました。
- VPCサブネット一覧を名前順で表示します。
- サービス利用権限に対応しました。

#### 不具合の修正

- モニタリンググラフの単位を修正しました。
    - 入力バイト: byte -> bytes/1min
    - 出力バイト: byte -> bytes/1min
    - 1秒あたりに処理したコマンド数: counts/seconds -> count/1sec
- プロフィールを修正する際、修正中のプロフィールを使用しているレプリケーショングループの状態が「プロフィール修正中」と表示されず、正常と表示される現象を修正しました。

### 2020. 08. 25.

#### 機能の改善・変更

- レプリケーショングループを作成する際、Redis接続用パスワード設定の有無をオプションで選択できるように変更しました。

#### 不具合の修正

- レプリカノードを昇格する際、ノード昇格完了時点とステータス表示が一致しない現象を修正しました。

### 2020. 07. 28.

#### 機能の追加

- インスタンスタイプの変更機能を追加しました。

#### 機能の改善・変更

- サポートされるインスタンスタイプからCompute Optimizedタイプを削除しました。

#### 不具合の修正

- モニタリンググラフで接続されたクライアントが累積で表示される現象を修正しました。
- ユーザー定義のプロフィールを使用していたRedis 3.2.12のレプリケーショングループのバックアップから復元する際、Redis 3.2のデフォルトプロフィールに復元されていたバグを修正しました。

### 2020. 06. 23.

#### 機能の改善・変更

- CloudTrailサービスにイベントを登録しました。

#### 不具合の修正

- ノードが再起動された場合、INFOのRedisサーバー稼働時間(uptime_in_seconds)が実際より9時間プラスされた値で出力される現象を修正しました。
- 接続情報タブでパスワードを確認しようとして表示ボタンをクリックしても、ノードが障害状態の場合はパスワードの取得に失敗する現象を修正しました。
- HAリセットボタンが表示され、HAリセットを実行したが失敗する現象を修正しました。
- モニタリング画面の検索期間で「指定」を選択しても期間を自由に設定できない現象を修正しました。

### 2020. 05. 26.

#### 機能の改善・変更

- Redis 5.0をサポートします。
- プロフィールのコピー機能を提供します。
- モニタリングを改善しました。
- 照会タブをレプリケーショングループの詳細画面から初期画面上部の別のタブに移動しました。
    - 現在時間ボタンを追加しました。
    - 自動更新チェックボックスを追加しました。
- レプリケーショングループ選択のプルダウンを追加しました。
- 単一のグラフをポップアップでも表示し、統計項目と集計期間を選択できるように改善しました。
- プロフィールを作成または修正する際、詳細設定から設定値を修正できるように変更しました。

#### 不具合の修正

- ノードが追加されたレプリケーショングループの修正後、ノードを追加していないレプリケーショングループを修正しようとすると、修正ボタンが無効になる現象を修正しました。
- モニタリング項目で接続されたクライアント数とブロックされたクライアント数が累積値で表示されるバグを修正しました。
- プロフィールリストの修正後、プロフィールリストが一部しか表示されない現象を修正しました。
- レプリケーショングループで使用中のプロフィールを修正した場合、修正後に画面がリロードされない現象を修正しました。

### 2020. 04. 28.

#### 機能の改善

- HA監視設定にヘルスチェック応答時間をユーザーが設定できる機能を提供します。
- マスター手動昇格機能を提供します。
- モニタリング項目で照会成功、失敗数を累積値ではなく時間別値として表示するように変更しました。
- アラームルールでメモリ使用量を%で設定できるように変更しました。

#### 不具合の修正

- 10分単位でモニタリングのグラフが途切れて表示される現象を修正しました。
- フェイルオーバー後、障害ノードが数秒以内に復旧した場合にレプリケーショングループ、ノードの状態が正常に反映されない現象を修正しました。
- 警告状態のレプリケーショングループのパスワード変更を可能に変更しました。
- 一部のメニューとラベルを修正しました。

### 2020. 03. 24.

#### 機能の改善

- EasyCacheの基本config値を変更しました(tcp-keepalive[0→300])。

### 2020. 02. 25.

#### 機能の改善

- 接続情報を同時に複数追加できるように修正しました。
- SMSの内容を改善しました。
- 重複したCIDR入力時のメッセージを修正しました。

#### 不具合の修正
- 大容量データがある時にレプリカノードを追加する場合、データ同期の無限ループが発生する問題を修正しました。
- レプリケーショングループ内のノード作成失敗時、ノード一覧でノードが作成中として表示され続ける問題を修正しました。

### 2020. 02. 11.

#### 機能の改善

- TOAST会員認証モジュールのバージョンをアップグレードしました。

#### 不具合の修正
- レプリケーショングループ作成時、インターネットゲートウェイを設定していないユーザーのVPCサブネットを選択した場合に失敗する現象を修正しました。

### 2020. 01. 21.

#### 機能の改善

- コンソール画面とイベントメッセージについて日本語をサポートします。
- フェイルオーバー(failover)後、ドメイン変更失敗時にイベント登録を追加しました。

### 2019. 12. 24.

#### 新規サービスリリース

- TOAST EasyCacheは、Redis(REmote DIctionary Server)をクラウド環境で提供するサービスです。
